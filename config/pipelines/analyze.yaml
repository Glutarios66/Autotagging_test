name: analyze
description: Deprecated compatibility alias for structured analysis and validation.
version: "1"
steps:
  - id: extract
    type: extract
    adapter: mock
  - id: analyze
    type: semantic_analysis
    adapter: mock
    needs: [extract]
  - id: validate
    type: validation
    adapter: mock
    needs: [analyze]
